enum OfficeName {
    GUILI
    FILDER_PERSONAL
}

model Office {
    id   String     @id
    name OfficeName

    createdAt    DateTime      @default(now())
    updatedAt    DateTime      @updatedAt
    applications Application[]

    @@map("office")
}
