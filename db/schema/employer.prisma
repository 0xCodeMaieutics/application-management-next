enum EmployerName {
    DHL

    @@map("employer_type")
}

model Employer {
    id      String       @id
    name    EmployerName
    address String

    createdAt   DateTime      @default(now())
    updatedAt   DateTime      @updatedAt
    applications Application[]

    @@map("employer")
}
